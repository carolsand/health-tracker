# Copy to .env and fill in real values. Never commit .env.
PORT=8317
SESSION_SECRET=replace-with-output-of: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DB_PATH=./data/tracker.db
NODE_ENV=production
